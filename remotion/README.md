# Animated five-bar chart

This folder contains a standalone Remotion composition named `FiveBarChart`.

## Preview

```bash
npm run remotion:studio
```

## Render an MP4

```bash
npm run remotion:render
```

The rendered file is written to `remotion/out/five-bar-chart.mp4`.
